services:
  server:
    build:
      context: ./server
    ports:
      - "8080:8080"
    env_file:
      - ./server/.env

  client:
    build:
      context: ./client
    ports:
      - "5173:5173"
    env_file:
      - ./client/.env
